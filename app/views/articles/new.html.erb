<%= provide(:title, "Create Knowledge") %>
	<div class="row">
		<div class="col-md-5">
			<h1>New Knowledge</h1>
				<%= render 'form' %>
				<%= link_to 'Back', business_articles_path(@business) %>
		</div>
	</div>

