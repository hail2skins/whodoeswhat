<%= provide(:title, "Edit Contact") %>
<div class="container">
	<div class="row">
		<div class="col-md-10">
			<h1>Edit Contact</h1>
				<%= render 'form' %>
				<%= link_to 'Back', business_contacts_path(@business) %>
		</div>
	</div>
</div>